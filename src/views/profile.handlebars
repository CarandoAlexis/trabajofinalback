<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8">
  <title>Perfil del Usuario</title>
</head>
<body>
  <h1>Lista de productos</h1>
  <p>Bienvenido/a, {{this.username}} {{this.first_name}} {{this.last_name}}. Tu rol es: {{this.role}}</p>
  <p> Aquí tienes la lista de productos:</p>
  <ul>
    {{#each products}}
    <li>
      <h3>{{title}}</h3>
      <p>{{description}}</p>
      <p>Precio: {{price}}</p>
      <p>Código: {{code}}</p>
      <p>Categoria: {{category}}</p>
    </li>
    {{/each}}
  </ul>

  <div>
    <form action="/api/session/logout" method="get">
      <p>
        <button type="submit">Cerrar Sesión</button>
      </p>
    </form>
  </div>
</body>
</html>